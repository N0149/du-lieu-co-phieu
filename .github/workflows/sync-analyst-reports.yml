name: Sync Analyst Reports Daily at 20:00

on:
  schedule:
    # Chạy tự động 1 ngày 1 lần lúc 13:00 UTC = 20:00 tối GMT+7 (Giờ Việt Nam)
    - cron: '0 13 * * *'
  workflow_dispatch: # Cho phép bấm nút "Run workflow" thủ công trên GitHub

jobs:
  sync-reports:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js 22 (Hỗ trợ node:sqlite)
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Run daily sync for Industry and Company reports
        run: |
          node scripts/sync-daily-reports.mjs

      - name: Commit and push updated reports
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/industry-reports.json data/industry_reports.db data/company_reports.db data/company_reports_summary.json data/widata_archive/reports/
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore(data): auto sync daily analyst reports $(date +'%Y-%m-%d %H:%M')" && git pull --rebase origin main && git push)

